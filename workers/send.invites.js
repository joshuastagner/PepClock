const email = require('./utils/email');


class InviteWorker {
  work () {
    // Find all invitations where sent is false

      // for each invitation generate link for event
        // store link for easy fetching

      // send emails
      
      // on success update invitation sent to true
   
  }
}

worker = new InviteWorker;

worker.work();
